{
  "name": "goodnight-css",
  "version": "1.0.0",
  "description": "Good Night CSS UI",
  "main": "index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "node-sass src/ --output dist/ --output-style=expanded --source-map=true",
    "build:watch": "node-sass src/ --output dist/ --output-style=expanded --source-map=true -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sandrolain/goodnight-css.git"
  },
  "keywords": [
    "css",
    "ui",
    "design",
    "system",
    "library",
    "components"
  ],
  "author": "Sandro Lain",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sandrolain/goodnight-css/issues"
  },
  "homepage": "https://github.com/sandrolain/goodnight-css#readme",
  "devDependencies": {
    "node-sass": "^7.0.0"
  }
}
